# Legacy Prototype Assets

更新：2026-06-23。

这里保存从 `prototype/` 移出的非运行资产：

- `Reference/`：早期 UI 参考截图。
- `Sample_ima/`：早期样例图片。

移动依据：

- 在当前 `prototype/*.html`、`src/`、`scripts/`、`public/`、根入口文档中未发现运行引用。
- `prototype/` 继续保留页面、头像、皮肤、游戏素材、embed 输出和 schema 参考，不改变可运行路径。

后续如果要删除这里的资产，先确认不再需要历史设计追溯。
